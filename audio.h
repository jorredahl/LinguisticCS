#ifndef AUDIO_H
#define AUDIO_H
#include "QByteArray"

class Audio
{
public:
    //QByteArray sourceAudio;
    //QByteArray userAudio;
    Audio();
    //Audio(QByteArray sourceAudio, QByteArray userAudio);
};

#endif // AUDIO_H
